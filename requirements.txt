apache-airflow==3.3.1
# Pinned, not floored, and for the reason the structlog note below records: a
# release nobody here made can turn this build red, and `>=` is an instruction
# to take it. The providers are where that risk actually lives - HITLOperator
# and LLMOperator are both young enough to move - so the version that was
# tested is the version that installs - 1.18.0 is what `>=1.10.0` had been
# quietly resolving to and what the suite actually runs against, so the pin
# records the tested version rather than the old floor. Dependabot proposes the
# bumps (see
# .github/dependabot.yml) and CI is what decides whether they land.
apache-airflow-providers-standard==1.19.0
apache-airflow-providers-common-ai==0.9.0
pydantic>=2.7
PyYAML>=6.0

# Airflow 3.1.0's logging setup imports Styles from structlog.dev, which
# structlog dropped in 25.5.0 - so a resolve from scratch installs 25.5+ and
# `import airflow` raises ImportError before any DAG is read. The published
# base image ships a working pin, which is why the in-container check passed
# and only a clean install exposed this. Airflow's own constraints file for
# 3.1.0 pins 25.4.0.
structlog<25.5
