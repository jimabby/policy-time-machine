apache-airflow==3.1.0
apache-airflow-providers-standard>=1.10.0
apache-airflow-providers-common-ai>=0.8.0
pydantic>=2.7
PyYAML>=6.0

# Airflow 3.1.0's logging setup imports Styles from structlog.dev, which
# structlog 26 removed - so a fresh resolve installs 26.x and `import airflow`
# raises ImportError before any DAG is read. The published base image already
# has a working pin; this is for every environment that resolves from scratch,
# CI included. Airflow's own constraints file for 3.1.0 pins 25.4.0.
structlog<26
